{
  "name": "circuit-breakers",
  "version": "1.0.0",
  "description": "circuit breaker server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start-server": "npm run build && node build/index.js",
    "test-breaker": "npm run build && node build/test.js"
  },
  "author": "vhutshilo",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.21"
  },
  "dependencies": {
    "axios": "^0.26.0",
    "express": "^4.17.3"
  }
}
